import React from 'react';
import {Container} from '@mui/material';
import SqlEditor from '../components/SqlEditor';

const Home: React.FC = () => {
    return (
        <Container>
            <SqlEditor/>
        </Container>
    );
};

export default Home;
